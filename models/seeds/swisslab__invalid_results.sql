
MODEL (
  name lth_bronze.swisslab__invalid_results,
  kind SEED (
    path '$root/seeds/swisslab__invalid_results.csv'
  ),
  description 'List of invalid results to filter out from SwissLab at LTH'
);
